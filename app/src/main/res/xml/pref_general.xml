<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="meteo_list"
        android:title="@string/settings_choose_meteo_station"
        android:defaultValue="-1"
        android:entries="@array/meteo_stations_list"
        android:entryValues="@array/meteo_stations_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
